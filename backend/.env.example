DATABASE_URL=postgresql+psycopg://postgres:YOUR_POSTGRES_PASSWORD@localhost:5432/replay

# Generate arandom JWT secret of at least 32 bytes long.
# Example:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=temporary-development-secret

OPENAI_API_KEY=not-set-yet
OPENAI_MODEL=

ENVIRONMENT=development