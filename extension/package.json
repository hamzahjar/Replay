{
  "name": "replay-extension",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/background.ts src/content.ts src/popup.ts --bundle --format=iife --outdir=dist --sourcemap",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}